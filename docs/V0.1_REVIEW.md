# mcp-census v0.1 — review before push

## Delta
- `src/stdio-list.mjs` — Content-Length JSON-RPC client: `initialize` → `notifications/initialized` → `tools/list`
- CLI: `--live`, `--allow-server <name>` (repeatable), `--timeout-ms`
- `fixtures/servers/fake-notes-mcp.mjs` + `fixtures/configs/live-notes.json` for offline CI of live path
- Tests: 8/8 including live allowlist + HTTP refuse
- README: Live tools/list section replaces roadmap stub

## Safety
- No HTTP/SSE remote spawn
- Explicit allowlist required; `*` rejected
- Fixtures still take precedence when `--tools-dir` provides tools
- Timeout + SIGTERM cleanup

## Verify
```bash
npm test
npm run demo:live
```

## Not in this PR
- HTTP_PROXY / remote MCP
- Auto-allow all servers
